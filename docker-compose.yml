version: '3.8'

services:
  tavern-keeper-db:
    image: postgres:15
    environment:
      POSTGRES_DB: tavern_keeper_db
      POSTGRES_USER: auth_user
      POSTGRES_PASSWORD: auth_pass
    ports:
      - "5402:5432"
    volumes:
      - auth_data:/var/lib/postgresql/data

volumes:
  auth_data:
